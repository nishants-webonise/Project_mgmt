<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task_description %><br />
    <%= f.text_field :task_description %>
  </div>
  <div class="field">
    <%= f.label :task_status %><br />
    <%= f.text_field :task_status %>
  </div>
  <div class="field">
    <%= f.label :task_priority %><br />
    <%= f.text_field :task_priority %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
