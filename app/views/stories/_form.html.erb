<%= form_for(@story) do |f| %>
  <% if @story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@story.errors.count, "error") %> prohibited this story from being saved:</h2>

      <ul>
      <% @story.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :story_name %><br />
    <%= f.text_field :story_name %>
  </div>
  <div class="field">
    <%= f.label :story_description %><br />
    <%= f.text_field :story_description %>
  </div>
  <div class="field">
    <%= f.label :story_status %><br />
    <%= f.text_field :story_status %>
  </div>
  <div class="field">
    <%= f.label :story_priority %><br />
    <%= f.text_field :story_priority %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
